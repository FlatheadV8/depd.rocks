{
  "name": "depd.rocks",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy src",
    "dev": "wrangler pages dev src --live-reload"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
